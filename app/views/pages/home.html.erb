<div class="rules container-md">
<p> HOW TO PLAY: </p>
<p> Using only the letters below, come up with the longest word. The more letters you use, the higher your score 🔝</p>
</div>
<div class="grid container-md">
<ul class="letters list-group list-group-horizontal">
<% @grid.each do |letter| %>
   <li class="list-group-item"> <%= letter %> </li>
 <% end %>
</ul>
<form action="/score" method="POST" data-turbo="false">
  <div class="form-group">
    <label for="word">Enter your word here</label>
     <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :grid, @grid %>
    <input type="text" class="form-control" name="word" id="word" placeholder="word...">
     <button type="submit" class="btn btn-primary">Submit</button>
  </div>
</form>
</div>
